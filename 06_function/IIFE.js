// IIFE Immediately Invoked Function  Expressions :

// Synatx () ()
(function chai() {
  console.log(`db connected`);
})(); // semicon use neigher error through becaues ; terminate tese fuction ;j

((name) => {
  console.log(`Db connected tow ${name}`);
})("avi");
